import{r as a}from"./config-renderer-BNPd5V4R.js";import"./primitives-Ejx4TyZa.js";import"./index-C54aaxDy.js";function e(o){return(t,f,p,u)=>a(o,t)}const r={title:"Normalization (RMSNorm / LayerNorm)",paramSummary:"d_model=512, norm_type=RMSNorm, eps=1e-6",inputTensor:"x: [b, seq, d_model]",outputTensor:"out: [b, seq, d_model]",steps:[{label:"Compute statistics",formula:"mu = mean(x), sigma^2 = var(x)",fill:"#e8f5e9",stroke:"#4caf50"},{label:"Normalize to zero-mean, unit-variance",formula:"x_hat = (x - mu) / sqrt(sigma^2 + eps)",fill:"#e3f2fd",stroke:"#1976d2"},{label:"Scale and shift (affine transform)",formula:"out = gamma * x_hat + beta",fill:"#f3e5f5",stroke:"#7b1fa2",matrices:[{label:"gamma",sublabel:"[d_model]",fill:"#f3e5f5",stroke:"#7b1fa2",w:80,h:30},{label:"beta",sublabel:"[d_model]",fill:"#fce4ec",stroke:"#d81b60",w:80,h:30}]}],explanations:[{text:"Normalization stabilizes training by keeping activations in a well-behaved range. RMSNorm drops the mean-centering step (assuming zero-mean is unnecessary) and is faster than LayerNorm, which is why Llama, Gemma, and most modern LLMs use RMSNorm.",type:"insight"},{text:"Cost is O(seq * d_model) per layer -- negligible compared to the O(seq * d_model^2) of attention and MLP. Params: 2 * d_model (gamma + beta) for LayerNorm, or d_model (gamma only) for RMSNorm.",type:"perf"}],paramCount:"2 * d_model (LayerNorm) or d_model (RMSNorm)",flops:"O(seq * d_model)",code:{title:"RMSNorm — Root Mean Square Layer Normalization (Zhang & Sennrich 2019)",desc:["Normalizes by the root-mean-square of activations, omitting mean-centering.","Used in Llama, Gemma, Mistral, and most modern decoder-only LLMs."],lines:["class RMSNorm(nn.Module):","    def __init__(self, d_model, eps=1e-6):","        self.gamma = nn.Parameter(torch.ones(d_model))","        self.eps = eps","","    def forward(self, x):","        rms = x.pow(2).mean(-1, keepdim=True).add(self.eps).rsqrt()","        return self.gamma * (x * rms)"]}},s={title:"Embedding (Token + Positional Encoding)",paramSummary:"vocab_size=32000, d_model=512, pos_encoding=RoPE",inputTensor:"token_ids: [b, seq]",outputTensor:"x: [b, seq, d_model]",steps:[{label:"Token embedding lookup",formula:"e = Embedding[token_id]  ->  [d_model]",fill:"#e8f5e9",stroke:"#4caf50",matrices:[{label:"E",sublabel:"[vocab, d_model]",fill:"#e8f5e9",stroke:"#4caf50",w:110,h:50}]},{label:"Positional encoding (RoPE / ALiBi / Sinusoidal)",formula:"RoPE: rotate(e, theta_i * pos)",fill:"#e3f2fd",stroke:"#1976d2"},{label:"Combine token + position",formula:"x = e (+ or rotate) PE(pos)",fill:"#fff3e0",stroke:"#e65100"},{label:"Scale embeddings",formula:"x = x * sqrt(d_model)",fill:"#f3e5f5",stroke:"#7b1fa2"}],explanations:[{text:"Embeddings map discrete token IDs to dense continuous vectors. The embedding matrix is the largest single parameter block in small models (vocab * d_model). In large models, MLP and attention parameters dominate.",type:"insight"},{text:"RoPE (Rotary Position Embedding) encodes position by rotating query/key vectors in d/2 complex planes. This gives attention scores that depend only on relative position, and is the default in Llama, Mistral, and Qwen.",type:"math"}],paramCount:"vocab_size * d_model",flops:"O(seq * d_model) for scaling",code:{title:"Token Embedding with RoPE (Su et al. 2021)",desc:["nn.Embedding is a simple lookup table; RoPE applies position-dependent rotations.","The scaling factor sqrt(d_model) compensates for the small magnitude of initialized embeddings."],lines:["self.embed = nn.Embedding(vocab_size, d_model)","","def forward(self, token_ids):","    x = self.embed(token_ids) * math.sqrt(d_model)","    x = apply_rope(x, positions)  # rotate in complex planes","    return x  # [batch, seq, d_model]"]}},n={title:"Mixture-of-Experts Layer",paramSummary:"n_experts=8, top_k=2, d_model=512, d_ff=2048",inputTensor:"x: [b, seq, d_model]",outputTensor:"out: [b, seq, d_model]",steps:[{label:"Router gate computes expert scores",formula:"G(x) = softmax(x * W_gate)",fill:"#e8f5e9",stroke:"#4caf50",matrices:[{label:"W_gate",sublabel:"[d_model, n_experts]",fill:"#e8f5e9",stroke:"#4caf50",w:100,h:45}]},{label:"Top-k expert selection",formula:"top_k(G(x)) -> {expert_i, weight_i}",fill:"#e3f2fd",stroke:"#1976d2"},{label:"Expert computation (per selected expert)",formula:"E_i(x) = W2_i * act(W1_i * x)",fill:"#f3e5f5",stroke:"#7b1fa2",matrices:[{label:"W1_i",sublabel:"[d_model, d_ff]",fill:"#f3e5f5",stroke:"#7b1fa2",w:70,h:45},{label:"W2_i",sublabel:"[d_ff, d_model]",fill:"#fce4ec",stroke:"#d81b60",w:70,h:45}]},{label:"Weighted sum of expert outputs",formula:"out = sum_k (gate_k * E_k(x))",fill:"#fff3e0",stroke:"#e65100"}],explanations:[{text:"Mixture-of-Experts increases model capacity without proportional compute increase. Mixtral 8x7B has 46.7B total parameters but only ~12.9B are active per token because only 2 of 8 experts are selected. This gives 70B-quality at 13B-compute.",type:"insight"},{text:"Only top_k experts process each token, so active FLOPs = top_k * FLOPs_per_expert. Load balancing loss ensures all experts are used equally. Expert parallelism shards experts across GPUs for distributed training.",type:"perf"}],paramCount:"d_model * n_experts + n_experts * 2 * d_model * d_ff",flops:"top_k * (4 * d_model * d_ff) per token",code:{title:"Sparse Mixture-of-Experts (Shazeer et al. 2017, Mixtral 2023)",desc:["Each token is routed to top-k experts; only selected experts compute outputs.","Load balancing loss prevents expert collapse (all tokens routing to same experts)."],lines:["self.gate = nn.Linear(d_model, n_experts, bias=False)","self.experts = nn.ModuleList([MLP(d_model, d_ff) for _ in range(n_experts)])","","def forward(self, x):","    scores = F.softmax(self.gate(x), dim=-1)    # [b, seq, n_experts]","    weights, indices = scores.topk(top_k, dim=-1)","    return sum(w * self.experts[i](x) for w, i in zip(weights, indices))"]}},i={title:"Residual Connection",paramSummary:"type=pre-norm, depth=32",inputTensor:"x: [b, seq, d_model]",outputTensor:"out: [b, seq, d_model]",steps:[{label:"Normalize input (Pre-Norm)",formula:"h = Norm(x)",fill:"#e8f5e9",stroke:"#4caf50"},{label:"Sublayer computation (Attention or MLP)",formula:"h = Sublayer(h)",fill:"#e3f2fd",stroke:"#1976d2"},{label:"Residual addition (skip connection)",formula:"out = x + h",fill:"#fff3e0",stroke:"#e65100"}],explanations:[{text:"Residual connections create direct gradient highways through the network, enabling training of very deep models (100+ layers). Without them, gradients would vanish or explode as they backpropagate through many nonlinear layers.",type:"insight"},{text:"Pre-Norm (norm before sublayer) is more stable than Post-Norm (norm after sublayer + add) and is the default in modern LLMs. DeepNorm scales residuals by alpha = (2N)^{1/4} and initializes sublayer outputs near zero, enabling 1000-layer Transformers.",type:"math"}],paramCount:"0 (no learned parameters in the skip connection itself)",flops:"O(seq * d_model) for the element-wise add",code:{title:"Pre-Norm Residual Block (Vaswani et al. 2017, Xiong et al. 2020)",desc:["Pre-Norm applies normalization before the sublayer, improving training stability.","The identity skip connection carries the original signal forward unchanged."],lines:["def forward(self, x):","    # Pre-Norm residual block","    h = self.norm(x)           # RMSNorm or LayerNorm","    h = self.sublayer(h)       # Attention or MLP","    return x + h               # Residual addition","","    # Post-Norm variant: return self.norm(x + self.sublayer(x))"]}},l={title:"Language Model Head",paramSummary:"d_model=512, vocab_size=32000, tied=false",inputTensor:"h: [b, seq, d_model]",outputTensor:"logits: [b, seq, vocab_size]",steps:[{label:"Project hidden states to vocabulary space",formula:"logits = h * W_out",fill:"#e8f5e9",stroke:"#4caf50",matrices:[{label:"W_out",sublabel:"[d_model, vocab_size]",fill:"#e8f5e9",stroke:"#4caf50",w:120,h:50}]},{label:"Temperature scaling (optional)",formula:"logits = logits / temperature",fill:"#e3f2fd",stroke:"#1976d2"},{label:"Softmax to get probabilities",formula:"probs = softmax(logits)",fill:"#f3e5f5",stroke:"#7b1fa2"},{label:"Sampling / greedy / beam search",formula:"token = sample(probs) or argmax(probs)",fill:"#fff3e0",stroke:"#e65100"}],explanations:[{text:"Tied weights (sharing W_out with the input embedding matrix E^T) saves d_model * vocab_size parameters. Most models (Llama, GPT-2) use tied weights, but some (PaLM, Gemma) untie them, finding that separate output projections improve quality at the cost of more memory.",type:"insight"},{text:"The LM head projection is a large matmul: d_model * vocab_size. For Llama-2 7B, that is 4096 * 32000 = 131M parameters (untied). The softmax over the full vocabulary is O(vocab) per position but is only computed at generation time for the last token.",type:"perf"}],paramCount:"d_model * vocab_size (untied) or 0 (tied to embedding)",flops:"O(seq * d_model * vocab_size) for projection",code:{title:"LM Head — Output projection to vocabulary logits",desc:["Projects the final hidden state to a score for every token in the vocabulary.","Tied weights share the embedding matrix; untied weights learn a separate projection."],lines:["# Untied head","self.lm_head = nn.Linear(d_model, vocab_size, bias=False)","","# Tied head (shares weight with nn.Embedding)","self.lm_head.weight = self.embed.weight  # W_out = E^T","logits = self.lm_head(h)                  # [b, seq, vocab_size]"]}},d={title:"Positional Encoding",paramSummary:"d_model=512, max_seq=8192, method=RoPE",inputTensor:"x: [b, seq, d_model]",outputTensor:"x_pe: [b, seq, d_model]",steps:[{label:"Compute position indices",formula:"pos = [0, 1, 2, ..., seq-1]",fill:"#e8f5e9",stroke:"#4caf50"},{label:"Generate positional encoding",formula:"RoPE: R(theta, pos); Sinusoidal: sin/cos(freq * pos)",fill:"#e3f2fd",stroke:"#1976d2",matrices:[{label:"PE / Theta",sublabel:"[seq, d_model] or [d/2]",fill:"#e3f2fd",stroke:"#1976d2",w:100,h:45}]},{label:"Apply PE to embeddings",formula:"x_pe = x (+ or rotate) PE",fill:"#f3e5f5",stroke:"#7b1fa2"},{label:"Optional: length extrapolation (YaRN / NTK-aware)",formula:"theta_scaled = theta * base^(factor)",fill:"#fff3e0",stroke:"#e65100"}],explanations:[{text:"Transformers process all positions in parallel and have no inherent notion of sequence order. Positional encodings inject this information. RoPE encodes relative position directly into attention scores, ALiBi adds a fixed bias that decays with distance, and sinusoidal encodings (original Transformer) use fixed frequency patterns.",type:"insight"},{text:"RoPE and ALiBi are O(seq * d_model) to compute and add no learned parameters. Learned positional encodings add max_seq * d_model parameters but may generalize poorly beyond training length. YaRN extends RoPE to longer contexts by rescaling the frequency base, enabling 128K+ context from 4K-trained models.",type:"perf"}],paramCount:"0 (RoPE/ALiBi/Sinusoidal) or max_seq * d_model (Learned)",flops:"O(seq * d_model)",code:{title:"RoPE — Rotary Position Embedding (Su et al. 2021)",desc:["Rotates pairs of dimensions in query/key vectors by angle theta_i * position.","The dot product of rotated Q and K naturally encodes relative position."],lines:["theta_i = 1.0 / (base ** (2i / d_model))   # frequency per dimension pair","angle = pos * theta_i                         # rotation angle at position","","def apply_rope(x, pos):","    cos, sin = cos(angle), sin(angle)","    x_even, x_odd = x[..., 0::2], x[..., 1::2]","    return stack(x_even*cos - x_odd*sin, x_even*sin + x_odd*cos)"]}},m={title:"Transformer Architecture Comparison",paramSummary:"Encoder-only vs Decoder-only vs Encoder-Decoder",inputTensor:"tokens: [b, seq]  (task-dependent)",outputTensor:"logits / representations  (task-dependent)",steps:[{label:"Encoder-only (BERT): bidirectional self-attention",formula:"h_i = Attend(all positions)",fill:"#e8f5e9",stroke:"#4caf50"},{label:"Decoder-only (GPT): causal masked self-attention",formula:"h_i = Attend(positions <= i)",fill:"#e3f2fd",stroke:"#1976d2"},{label:"Encoder-Decoder (T5): bidirectional enc + cross-attn dec",formula:"h_dec = CrossAttend(h_dec, h_enc)",fill:"#f3e5f5",stroke:"#7b1fa2"},{label:"Architecture selection based on task",formula:"classify -> encoder; generate -> decoder; seq2seq -> enc-dec",fill:"#fff3e0",stroke:"#e65100"}],explanations:[{text:"Encoder-only models (BERT, RoBERTa) see all positions bidirectionally, excelling at understanding tasks. Decoder-only models (GPT, Llama) use causal masking for autoregressive generation and now dominate LLM research. Encoder-Decoder models (T5, BART) handle sequence-to-sequence tasks like translation and summarization naturally.",type:"insight"},{text:"Decoder-only architectures have won the scaling race: they unify understanding and generation in a single framework, simplify training (one loss function), and scale predictably. Encoder-Decoder can be more parameter-efficient for translation but requires two forward passes.",type:"perf"}],paramCount:"N_layers * (4 * d_model^2 + 2 * d_model * d_ff)  per stack",flops:"Encoder: O(s^2 * d); Decoder: O(s^2 * d) with causal mask",code:{title:"Architecture Paradigms — BERT, GPT, T5",desc:["The key difference is the attention mask: bidirectional, causal, or both.","Modern LLMs are overwhelmingly decoder-only for unified pre-training and generation."],lines:["# Encoder-only (BERT): full bidirectional attention","attn_mask = None                         # attend to all positions","","# Decoder-only (GPT): causal mask","attn_mask = torch.tril(ones(seq, seq))   # attend to past only","","# Encoder-Decoder (T5): cross-attention","h_dec = decoder_cross_attn(q=h_dec, k=h_enc, v=h_enc)"]}},c={title:"Retrieval-Augmented Generation (RAG)",paramSummary:"embed_dim=768, top_k=5, chunk_size=512",inputTensor:"query: [b, seq_q]  (user question)",outputTensor:"answer: [b, seq_a]  (generated response)",steps:[{label:"Encode query into dense embedding",formula:"q_vec = Encoder(query)  ->  [embed_dim]",fill:"#e8f5e9",stroke:"#4caf50",matrices:[{label:"Encoder",sublabel:"[d_model, embed_dim]",fill:"#e8f5e9",stroke:"#4caf50",w:90,h:45}]},{label:"Retrieve top-k relevant documents",formula:"docs = argmax_k(cosine(q_vec, D))",fill:"#e3f2fd",stroke:"#1976d2"},{label:"Augment context: concatenate retrieved docs with query",formula:"context = [doc_1; doc_2; ...; doc_k; query]",fill:"#f3e5f5",stroke:"#7b1fa2"},{label:"Generate answer conditioned on augmented context",formula:"answer = LLM.generate(context)",fill:"#fff3e0",stroke:"#e65100"},{label:"Optional: iterative retrieval / re-ranking",formula:"re-rank(docs, query) -> refined context",fill:"#e0f2f1",stroke:"#00838f"}],explanations:[{text:"RAG combines parametric knowledge (stored in model weights) with non-parametric knowledge (retrieved from an external corpus). This lets the model access up-to-date information, reduce hallucinations, and cite sources -- without retraining.",type:"insight"},{text:"Retrieval latency dominates RAG performance. Dense retrieval (ANN search with FAISS/HNSW) achieves ~10ms for top-k search over millions of documents. Chunking strategy (overlap, size, semantic boundaries) and embedding quality are the two biggest accuracy levers.",type:"perf"}],paramCount:"Encoder params + LLM params + index (embed_dim * n_docs)",flops:"O(n_docs * embed_dim) for brute-force search; O(log(n_docs)) for ANN",code:{title:"RAG Pipeline — Lewis et al. 2020",desc:["Retrieval step uses a bi-encoder: separate encoders for queries and documents.","The retrieved passages are prepended to the prompt for generation."],lines:["query_emb = retriever.encode(query)        # [embed_dim]","doc_ids = index.search(query_emb, top_k=5) # approximate NN search","context = [corpus[i] for i in doc_ids]","",'prompt = "\\n".join(context) + "\\nQ: " + query',"answer = llm.generate(prompt)              # autoregressive decoding"]}},_=e(r),x=e(s),y=e(n),k=e(i),q=e(l),v=e(d),w=e(m),E=e(c);export{w as renderArchitectureComparisonDetailed,x as renderEmbeddingDetailed,q as renderLmHeadDetailed,y as renderMoeDetailed,_ as renderNormDetailed,v as renderPositionalEncodingDetailed,E as renderRagDetailed,k as renderResidualDetailed};
//# sourceMappingURL=core-renderers-Cd4rS5q0.js.map
